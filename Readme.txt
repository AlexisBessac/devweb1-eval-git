 Repertoriez toutes les commandes git saisies

 git init

 git add .

 git commit -a -m "First commit"

git remote add origin https://github.com/AlexisBessac/devweb1-eval-git.git
git branch -M master
git push -u origin master

git commit -a -m "ajout d'une commande"

git push